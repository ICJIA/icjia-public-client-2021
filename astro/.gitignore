# build output
dist/
# generated types
.astro/
# netlify adapter / local netlify state
.netlify/
# deps
node_modules/
# env
.env
.env.production
.env.local
# logs
*.log
# visual-regression artifacts (captures + diffs are large; keep masks/config in git)
scripts/vr/__captures__/
scripts/vr/__diffs__/

# build-time-extracted hub images (regenerated by prebuild)
public/hub-images/
public/hub-images-manifest.json

# prebuild content artifacts (regenerated each build by generate-search-index.mjs)
public/searchIndex.json
public/sitemap.xml

# agent/scratch probe files at the astro root (real tests are vitest *.test.ts)
test_*.mjs
/*.test.mjs
